[build]
  command = "npm run build"
  publish = "dist"

# Same as public/_redirects — rewrite unknown paths to the SPA entry (not a 404).
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
